//! rauc-rs binary — placeholder CLI

fn main() {
    println!("rauc-rs v{}", env!("CARGO_PKG_VERSION"));
    println!("This binary is a placeholder. Use the library directly.");
    println!("Or use the system 'rauc' CLI for RAUC operations.");
}
